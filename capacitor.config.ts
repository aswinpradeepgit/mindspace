import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mindspend.app',
  appName: 'MindSpend',
  webDir: 'out',
  android: {
    backgroundColor: '#FDF7FF',
  },
};

export default config;
